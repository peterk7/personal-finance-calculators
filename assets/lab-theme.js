/* Finance Lab theme for Chart.js — include after the Chart.js CDN
   script and before the page's own chart code. */
if (window.Chart) {
  Chart.defaults.color = "#8b9bb4";
  Chart.defaults.borderColor = "rgba(139, 155, 180, 0.18)";
}
